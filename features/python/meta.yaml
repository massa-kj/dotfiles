description: Python runtime with uv package manager and language server
depends:
  - mise

runtimes:
  # version resolved from profile config (DOTFILES_FEATURE_CONFIG_VERSION)
  - name: python
  # uv is always installed at latest
  - name: uv
    version: latest
